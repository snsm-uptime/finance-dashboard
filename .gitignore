# OS
.DS_Store

# Secrets & local env
.env
.env.local
.env.*.local
*.pem

# Operator data (real statements / PII — never commit)
bank_data/
**/bank_data/

# statement_recon.py output (safe/anonymized, but scratch by default —
# `git add -f` a specific file to promote it into a real test fixture)
api/tests/fixtures/bank_recon/

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/
.coverage
htmlcov/

# Node / Next
node_modules/
.next/
out/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE / tooling
.idea/
.vscode/
*.swp
*.swo

# Docker / local overlays
docker-compose.override.yml

# Accidental in-repo Postgres bind (must stay outside the repo)
/pgdata/

# Cursor worktree bootstrap (override common global **/.cursor/ ignores)
!.cursor/
!.cursor/worktrees.json
