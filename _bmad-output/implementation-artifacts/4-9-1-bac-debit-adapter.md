---
baseline_commit: ba29215
---

# Story 4.9.1: BAC debit adapter (first SIGN_VARIANT implementation)

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a developer extending statement import to deposit accounts,
I want a `BacDebitAdapter` that recognizes real BAC debit statements (COLONES and DOLARES) and resolves DÉBITOS/CRÉDITOS by physical column position,
so that debit-account uploads parse into `CanonicalLine` rows using the AD-28 `SIGN_VARIANT` contract this story implements for the first time.

## Acceptance Criteria

1. **Given** a real BAC debit statement (`BAC_DEB_COLONES_jun.pdf`, `BAC_DEB_DOLARES_jun.pdf`), **when** `BacDebitAdapter`'s content sniff runs, **then** it detects the statement via a marker unique to debit statements (e.g. "CUADRO RESUMEN" + "DÉBITOS CRÉDITOS SALDOS") — distinct from BAC credit's `ESTADO DE CUENTA BAC CREDITO` marker.
2. **Given** `BacDebitAdapter` declares `date_format` per AD-26, **when** a row's date token is read (e.g. `JUN/01`, `MAY/29`), **then** it is parsed with the `"%b/%d"` format — no year token — using the PDF's `/CreationDate` metadata as `reference_date` and AD-26's nearest-prior-year resolution, never a private per-row heuristic.
3. **Given** `BacDebitAdapter` declares `AmountColumnRole = SIGN_VARIANT` per AD-28, **when** a row carries exactly one amount token with no textual DB/CR marker, **then** the adapter resolves DÉBITOS vs. CRÉDITOS by comparing the token's x-position midpoint against two declared, non-overlapping x-position ranges (one per column) — the smallest-distance range wins, per AD-28's fixed-metric rule.
4. **Given** AD-28's construction-time validation rule, **when** `BacDebitAdapter` is constructed with missing, non-1:1, or overlapping range declarations, **then** it raises at construction (`__init__`/module load), never deferring the check to `parse()`.
5. **Given** BAC debit statements print one continuous transaction table with no lettered section headers (unlike BAC credit's A–G sections), **when** `BacDebitAdapter` classifies rows, **then** it uses the shared `domain/statement_row_extraction.py` date+amount-token classifier (AD-28) directly against the `NO. REFERENCIA FECHA CONCEPTO DÉBITOS CRÉDITOS` table, with `CUADRO RESUMEN` (before) and `ÚLTIMA LÍNEA SALDO AL CORTE` (after) treated as non-data boilerplate, not parsed as transaction rows.
6. **Given** `BacDebitAdapter`'s currency handling, **when** a COLONES vs. a DOLARES statement is parsed, **then** both are accepted by the same adapter — currency is read from the statement's `Moneda:` field, not a separate adapter per currency.
7. **Given** the CI synthetic fixture gate (mirrors Story 4.9's pattern), **when** a BAC debit fixture is generated, **then** it uses real section-free table text shape and real date/amount token shapes matching real pdfplumber extraction, with goldens covering both a DÉBITOS-only and a CRÉDITOS-present row.

## Tasks / Subtasks

- [x] **Task 1: Add `SIGN_VARIANT` resolution to `domain/statement_row_extraction.py`** (AC: #3, #4)
  - [x] Write domain tests first (red → green, mirror `test_statement_row_extraction_domain.py`'s existing style). `AmountColumnRole.SIGN_VARIANT` already exists in this module (added unused by Story 4.9) — this story adds the actual resolution function, not the enum.
  - [x] Add a resolver, e.g. `resolve_sign_variant_column(x0: float, x1: float, ranges: Mapping[str, tuple[float, float]]) -> str`, that: computes the token midpoint `(x0+x1)/2`, computes each declared range's midpoint, and returns the key of the range with the smallest absolute distance to the token midpoint (AD-28's fixed metric — never "inside range" containment, which would fail to resolve a token that falls just outside a range due to the documented page-to-page drift).
  - [x] Add a validation helper (or a small dataclass, e.g. `SignVariantRanges`) that raises `ValueError` (or a domain error — match existing conventions in this module/`domain/errors.py`) when ranges are missing, not exactly 1:1 with declared outcomes, or overlapping. `BacDebitAdapter` calls this at construction time (Task 3) — the validation logic itself belongs in domain (AD-28 frames this as the shared contract), the *call site* raising at adapter construction is the adapter's job.
  - [x] Pure domain: no `pdfplumber` import. Inputs are plain floats/strings the adapter already extracted via `pdfplumber` word data (AD-1 — domain never imports pdfplumber; adapter hands it `(text, x0, x1)` tuples per AD-28's own wording).

- [x] **Task 2: Create `api/adapters/bank/bac_debit/` module** (AC: #1, #2, #5, #6)
  - [x] New package: `api/adapters/bank/bac_debit/__init__.py` (empty or re-export, mirror `bac_credit/__init__.py`), `api/adapters/bank/bac_debit/adapter.py`.
  - [x] `BacDebitAdapter` class: `bank_id = "bac"`, `product_id = "bac_debit"`, `account_kind = "debit"` (or `"checking"`/`"deposit"` — pick one; `bac_credit` uses `"credit"`, no other `account_kind` value exists yet in the codebase, so this is a new value this story introduces; keep it descriptive and consistent with card-registration UI copy if any exists — grep `account_kind` usages before picking).
  - [x] `detect()`: content-sniff marker distinct from BAC credit's `"ESTADO DE CUENTA BAC CREDITO"`. Real evidence (this session, `bank_data/BAC_DEB_COLONES_jun.pdf`, page 0): the debit statement prints `"CUADRO RESUMEN"` followed by `"DÉBITOS CRÉDITOS SALDOS"` on the next line, and never prints BAC credit's marker string. Use `sniff_content_marker` from `adapters/bank/_shared.py` (same helper `BacCreditAdapter` uses) — pick one reliable single-string marker, or extend the check if a single string proves ambiguous against BAC credit's own statement (BAC credit statements do NOT contain "CUADRO RESUMEN" — confirm this holds, but it's a reasonable choice: that block appears immediately after the interest-rate table specific to debit/savings products). Filename fallback: `"bac" in filename.lower()` is already used by `BacCreditAdapter` and is ambiguous between the two BAC products — do **not** rely on filename alone to distinguish debit from credit; content sniff must be authoritative when both filename-only matches fire (this shapes how `detect_bank_adapter`'s priority chain in `api/application/bank_adapters.py` behaves — an ambiguous filename match across two BAC adapters is expected to fall through to content-sniff resolution, per that module's existing priority logic; do not change `bank_adapters.py`).
  - [x] `date_format = "%b/%d"` per AD-26. Reuse `parse_statement_date` from `domain/statement_dates.py` — it already resolves a missing-year format via `reference_date` (nearest-prior-year). No new date-parsing logic needed; this is Story 4.9's already-proven `parse_pdf_creation_date`/`statement_reference_date` pattern (`bac_credit/adapter.py:115-133,169-185`) reused for a new product. **Caveat:** the real `BAC_DEB_COLONES_jun.pdf`/`BAC_DEB_DOLARES_jun.pdf` PDFs inspected this session have **empty pdfplumber metadata** (`doc.metadata == {}`, confirmed) — same as BAC credit's real PDFs (per Story 4.9's Dev Agent Record). The statement prints `"Fecha de Corte:"` header text and a printed cut-off date like `"30/JUN/26"` (DD/MMM/YY, note: different token order/format than the row date format) on page 0. Mirror `BacCreditAdapter.statement_reference_date`'s pattern: prefer a printed reference date over `/CreationDate` when both might be empty/absent — write a `bac_debit`-specific printed-date regex for `"Fecha de Corte:"` / `"Fecha de corte:"` (case appears inconsistently across pages in the real sample — page 0 has "Fecha de Corte:", page 1 has "Fecha de corte:") since the format (`DD/MMM/YY` with slashes) differs from BAC credit's `Fecha de emisión:` (`DD-MMM-YY` with dashes) and cannot reuse `_ISSUANCE_DATE_RE` as-is.
  - [x] `split()`: reuse `detect_statement_boundaries` from `domain/statement_layout.py` exactly as `BacCreditAdapter.split()` does (AD-27, unchanged, do not modify). Pass the same detect() marker as the boundary marker.
  - [x] `parse()`: no `SectionSpec`/`SectionCursor` needed — AC #5 explicitly says this product has one continuous table, no lettered sections. Use `is_data_row`/`extract_row_tokens` from `domain/statement_row_extraction.py` directly against every stripped line. Treat `"CUADRO RESUMEN"` through the `"NO. REFERENCIA FECHA CONCEPTO DÉBITOS CRÉDITOS"` column-header line as non-data preamble (skip everything before the column header is seen), and `"ÚLTIMA LÍNEA SALDO AL CORTE"` (and everything after) as trailing boilerplate (stop parsing once seen). **Real preamble also contains a data-row-shaped decoy line** — confirmed this session on `BAC_DEB_COLONES_jun.pdf` page 0: `'42 2019,411.82 8 1644,280.26 487,997.97 1,309,316.43 934,184.87'` (the `CUADRO RESUMEN` totals line) contains multiple amount-shaped tokens and could false-positive as a data row if the column-header gate isn't strict — gate on "have we seen the `NO. REFERENCIA...` column-header line yet," not just on "does `is_data_row` return true."
  - [x] Amount extraction: real rows carry exactly **one** amount token (per AC #3 — "a row carries exactly one amount token with no textual DB/CR marker", confirmed against real extraction this session — no row inspected had two amount tokens or a `DB`/`CR` text tag). Get the token's `(x0, x1)` via `pdfplumber`'s `page.extract_words()` (not `extract_text()`, which loses geometry) — the adapter must re-locate the matched amount substring's word object to read its x-position; `BacCreditAdapter` never does this today (it's pure regex-over-text), so this is new plumbing this story builds, not a reuse.
  - [x] Currency: read from the statement's `"Moneda:"` field (e.g. `"Moneda: COLONES"` → `CRC`, confirmed real text this session; the DOLARES statement is expected to print `"Moneda: DOLARES"` → `USD` — verify against `BAC_DEB_DOLARES_jun.pdf` during implementation, not assumed). One adapter instance handles both — do not branch on filename.
  - [x] `line_type` mapping: this codebase's `domain/line_types.py` has no existing "deposit"/"withdrawal"/"debit-account" line type — every current type (`purchase`, `payment`, `interest`, `fee`, `voluntary_service`, `credit_note`, `installment_schedule`, `balance_forward`, `other`, `classified_purchase_reversal`) was coined for the credit-card product. This story must pick line types for DÉBITOS (money leaving the account) and CRÉDITOS (money entering) rows and is expected to add new ones if none fit — **do not silently force debit-account rows into `LINE_TYPE_PURCHASE`** (that constant is credit-card-specific and `domain/settle.py`'s `INCLUDED_LINE_TYPES = frozenset({LINE_TYPE_PURCHASE, LINE_TYPE_CLASSIFIED_PURCHASE_REVERSAL})` gates what settle-up math includes — silently reusing `LINE_TYPE_PURCHASE` would pull every debit-account row into shared-expense settle math, which is very likely wrong for a personal checking account import with no product decision made about it). This story's scope is adapter output (`CanonicalLine` rows), not settle-math wiring — pick/add line type(s) that describe the semantics honestly (e.g. new `LINE_TYPE_DEBIT`/`LINE_TYPE_CREDIT` or `LINE_TYPE_WITHDRAWAL`/`LINE_TYPE_DEPOSIT` in `domain/line_types.py`, added to `LINE_TYPES` frozenset), leave `settle.py`/`INCLUDED_LINE_TYPES` **untouched** (no ACs here require settle integration), and document the choice and its rationale in Dev Notes/Completion Notes the same way Story 4.9 documented its undetermined-by-PRD sign convention (`bac_credit/adapter.py:88-90`) — this is a real open product decision, not a detail to invent silently and bury.
  - [x] Sign: DÉBITOS rows should be negative (money leaving) or positive per whatever convention is chosen above — be internally consistent and explicit, mirroring `_signed_amount`/`_NEGATIVE_LINE_TYPES` in `bac_credit/adapter.py:91,106-107`.
  - [x] `amount_column_role = AmountColumnRole.SIGN_VARIANT`. Declare the two named x-position ranges (DÉBITOS, CRÉDITOS) as class-level constants, validated at construction via Task 1's validator — raise in `__init__` per AC #4.
  - [x] Preserve fail-loud behavior: a row that doesn't resolve to either declared range, or a description-only preamble/footer line matched by `is_data_row` outside the column-header gate, must not silently produce a wrong or dropped row — follow `fail_parse` from `adapters/bank/_shared.py` (same helper `BacCreditAdapter.parse()` uses) for any row that can't be classified.

- [x] **Task 3: Register the new adapter** (AC: #1)
  - [x] Add `BacDebitAdapter` import + registry entry in `api/adapters/bank/__init__.py`'s `ADAPTERS` list, alongside `BacCreditAdapter()` and `PromericaStubAdapter()`. Per this file's own docstring, this should require **zero edits** to `application/bank_adapters.py` or `domain/canonical_line.py` (FR-31/FR-36 proof) — if this story finds itself needing to touch either, stop and reconsider the approach before proceeding, that would indicate the `BankAdapter` protocol doesn't actually cover this case.

- [x] **Task 4: Build synthetic fixture + goldens** (AC: #7)
  - [x] New `api/scripts/generate_bac_debit_fixture.py` (mirror `generate_bac_fixture.py`'s structure/approach) emitting the real section-free table shape: `"NO. REFERENCIA FECHA CONCEPTO DÉBITOS CRÉDITOS"` header line, then rows shaped like the real ones (reference number, `MMM/DD` date, description, then **one** amount positioned at either the DÉBITOS or CRÉDITOS physical column). Must produce PDF word geometry a real `pdfplumber.extract_words()` call will see with x-positions actually falling into the two declared ranges — verify by round-tripping the generated fixture through `pdfplumber` before trusting it, the same integration-honesty concern Story 4.9's Dev Notes flagged for its own fixture generator.
  - [x] Fixture must include **both** a DÉBITOS-only row and a CRÉDITOS-present row (AC #7 explicit requirement) — real data this session skewed heavily DÉBITOS (personal expense account), so don't just mirror the real sample's imbalance; construct a synthetic CRÉDITOS row deliberately (e.g. an incoming transfer/deposit) so the SIGN_VARIANT resolution path is actually exercised both ways in CI.
  - [x] `api/tests/fixtures/pdf/bac_debit_synthetic.pdf` + `bac_debit_synthetic_goldens.py` (mirror `bac_credit_synthetic_goldens.py`'s shape — a module of expected `CanonicalLine` field values, not hand-asserted inline in the test file).
  - [x] New `api/tests/test_bac_debit_adapter.py` mirroring `test_bac_adapter.py`'s coverage: `detect()` true/false cases (including: does it correctly NOT match a BAC credit fixture, and vice versa — construction-time range-validation error cases per AC #4 — `SIGN_VARIANT` resolution correctness for both columns — date parsing via the printed reference-date fallback — currency read from `Moneda:` for both COLONES and DOLARES — fail-loud on an unresolvable row.

- [x] **Task 5: Full regression pass** (AC: all)
  - [x] Run the full `api` pytest suite. Confirm zero changes to `application/`, persistence, commit/dedup logic, and (per Task 2's line-type note) zero changes to `domain/settle.py`'s `INCLUDED_LINE_TYPES` unless a deliberate, documented decision is made to include the new line type(s) in settle math — default to NOT touching it; this story's ACs are adapter-output-only.
  - [x] `ruff check`/`ruff format` clean (Story 4.9's Task 6 precedent).
  - [x] If real PDFs are available locally (`bank_data/`, gitignored, never committed — both `BAC_DEB_COLONES_*.pdf` and `BAC_DEB_DOLARES_*.pdf` months exist), a manual smoke-test parse is encouraged but is **not** a merge gate — CI only gates on the synthetic fixture (project-context.md: "operator real PDFs never in repo/CI, never block merge").

## Dev Notes

- **Why this story exists / what it completes:** Story 4.9 (`4-9-bac-credit-real-statement-compatibility-fix.md`) explicitly deferred `SIGN_VARIANT` as out of scope (its AC #6) and implemented only `CURRENCY_VARIANT`. `AmountColumnRole.SIGN_VARIANT` was added to `domain/statement_row_extraction.py` by that story as a declared-but-unused enum member. This story is the first real consumer of that half of the contract — see Sprint Change Proposal 2026-09-22 (`_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-22.md`) for the full trigger/impact analysis.
- **Architecture contracts this story implements against — read AD-26 and AD-28 in full** (`ARCHITECTURE-SPINE.md`, lines ~297-318): AD-26 (per-product date-format contract, `%b/%d` with `/CreationDate` fallback) and AD-28 (real-text row recognition + amount-column role, specifically the `SIGN_VARIANT` half this story is the first to implement). Both ADs already fully specify the contract shape based on real-statement evidence gathered before this story was written — this is implementation, not design.
- **Real-PDF evidence gathered this session** (`bank_data/BAC_DEB_COLONES_jun.pdf`, gitignored, structural facts only, never committed):
  - `doc.metadata` is `{}` (empty) — same gap BAC credit hit; do not assume `/CreationDate` will be populated, mirror `BacCreditAdapter.statement_reference_date`'s printed-date-first fallback pattern.
  - Page 0 prints (in order): interest-rate table boilerplate → `Cuenta IBAN:` / `Moneda: COLONES` / `Fecha de Corte:` header block → `CUADRO RESUMEN` → `DÉBITOS CRÉDITOS SALDOS` → a totals line containing multiple amount-shaped tokens (a decoy for naive `is_data_row` gating, see Task 2) → `NO. REFERENCIA FECHA CONCEPTO DÉBITOS CRÉDITOS` (the real column-header line, exact casing/spacing) → transaction rows, e.g. `'053100000 MAY/29 IVA -UBER *TRIP-HELP.UB 284.26'`, `'000000002 JUN/01 Sobrantesporrefundiciondec 50,000.00'`.
  - Page 1 (continuation, no repeated `CUADRO RESUMEN`) ends with `ÚLTIMA LÍNEA SALDO AL CORTE 934,184.87` then footer boilerplate.
  - Two-column word geometry confirmed via `pdfplumber.extract_words()` this session on this exact real page: the `"NO. REFERENCIA FECHA CONCEPTO DÉBITOS CRÉDITOS"` header prints the **transaction-table** DÉBITOS at `x0≈467.5–502.5` and CRÉDITOS at `x0≈539.4–580.6` (a second, earlier `DÉBITOS`/`CRÉDITOS` pair at `x0≈71.8–111.2` / `x0≈211.8–258.2` belongs to the unrelated `CUADRO RESUMEN` summary table — do not confuse the two when picking which header occurrence to anchor ranges against). Every inspected amount token's `x0` fell in one of two clusters: `≈483.9–519.0` and `≈558.9–594.0` — these clusters are what the adapter's declared ranges must cover, but **measure directly against the actual fixture/PDF at implementation time** rather than hardcoding these exact session-observed numbers verbatim (AD-28 itself warns of page-to-page drift — confirm ranges are stable across both real files and the synthetic fixture, don't just copy this paragraph's numbers in blind).
  - No row inspected had a textual `DB`/`CR` marker or two amount columns — confirms AC #3's "exactly one amount token with no textual DB/CR marker" premise.
- **Existing code to read completely before touching:**
  - `api/adapters/bank/bac_credit/adapter.py` (all of it) — the sibling adapter this one is structurally modeled on: `detect()`, `split()`, `statement_reference_date()`/`parse_pdf_creation_date()`/`parse_printed_issuance_date()` (reuse the *pattern*, not literally these BAC-credit-specific regexes — the printed-date label text and format differ), `parse()`'s preamble/boilerplate skip logic, `_shared.py` usage.
  - `api/domain/statement_row_extraction.py` (all of it, already read this session) — `is_data_row`, `extract_row_tokens`, `AmountColumnRole`, `DEFAULT_AMOUNT_PATTERN`. This story's Task 1 adds to this file; it does not replace anything in it.
  - `api/domain/statement_dates.py` (all of it, already read this session) — `parse_statement_date`'s `%y`-absent/`reference_date` nearest-prior-year path is exactly what AC #2 requires; no changes needed to this file.
  - `api/domain/canonical_line.py` — `CanonicalLine`, `validate_canonical_line`, `LINE_TYPES`/`SECTION_POLICIES`. If new line types are added (Task 2), they go in `domain/line_types.py`'s `LINE_TYPES` frozenset, not here.
  - `api/domain/line_types.py` (all of it, already read this session) — no debit/credit-account-generic type exists today; see Task 2's line-type note.
  - `api/domain/settle.py` — `INCLUDED_LINE_TYPES` (do not touch unless a deliberate, documented decision is made — see Task 2/5).
  - `api/adapters/bank/_shared.py` (all of it, already read this session) — `fail_parse`, `parse_amount_field`, `sniff_content_marker`. Reuse as-is; BAC debit amounts observed this session have no trailing-minus notation (unlike BAC credit's interest section) — confirm this holds for CRÉDITOS rows during implementation rather than assuming.
  - `api/adapters/bank/__init__.py` — the registry this story's Task 3 edits (one line + one import).
  - `api/scripts/generate_bac_fixture.py` and `api/tests/fixtures/pdf/bac_credit_synthetic_goldens.py` — the pattern Task 4's new debit-equivalent files mirror (word-position-aware PDF generation, since this fixture must carry real x-position geometry, which the credit fixture generator does not need to).
  - `api/tests/test_bac_adapter.py` — the pattern Task 4's new `test_bac_debit_adapter.py` mirrors (imports, fixture-loading helper, golden-comparison style).
- **Testing standard (project-context.md, non-negotiable):** parsers/domain get red → green TDD. Money assertions use `Decimal`, never `float`. CI gates only on the synthetic fixture + goldens; real PDFs never enter the repo or CI and never block merge (both are already gitignored under `bank_data/` — confirm before any accidental `git add`).
- **No settle/UI/persistence work in this story.** Everything stays inside `api/adapters/bank/bac_debit/` (new), one addition to `api/domain/statement_row_extraction.py`, a possible addition to `api/domain/line_types.py`, `api/adapters/bank/__init__.py` (registry), `api/scripts/generate_bac_debit_fixture.py` (new), and `api/tests/`. Matches AD-1's adapter boundary (bank adapters emit `CanonicalLine` only) and this story's own explicit scope (adapter, not settle wiring).
- **No new external dependency** — reuses `pdfplumber` (already pinned, and this story is the first adapter to need its word-level `x0`/`x1` geometry output rather than just `extract_text()`) and Python's `re`/`decimal` stdlib.
- **Previous story intelligence (4.9, same epic, immediately prior):** confirmed real BAC PDFs have empty pdfplumber metadata — do not assume `/CreationDate` works; build the printed-date fallback first. Story 4.9 also established the "document an undetermined-by-PRD convention explicitly in the adapter's own comments, applied consistently" pattern (its sign convention) — this story's line-type/sign choices should follow the same convention rather than silently picking without a trace. Story 4.9's git/PR history shows code-review follow-up commits rather than amends — follow the same convention if review findings come back on this story.
- **Recent commits relevant to this story:** `ba29215` "archive spec" (unrelated, latest on `main`); the BAC credit fix shipped across PRs #61–#63 under branch names still carrying the pre-renumbering `4-11` — not relevant to this story's branch naming (use `<type>/4/4-9-1-...` per the current numbering, no renumbering confusion applies here since this story was never renumbered).

### Project Structure Notes

- New files: `api/adapters/bank/bac_debit/__init__.py`, `api/adapters/bank/bac_debit/adapter.py`, `api/scripts/generate_bac_debit_fixture.py`, `api/tests/fixtures/pdf/bac_debit_synthetic.pdf`, `api/tests/fixtures/pdf/bac_debit_synthetic_goldens.py`, `api/tests/test_bac_debit_adapter.py`.
- Modified files: `api/domain/statement_row_extraction.py` (add `SIGN_VARIANT` resolver), `api/adapters/bank/__init__.py` (registry), possibly `api/domain/line_types.py` (new line type(s) — see Dev Notes).
- No `ui/` changes. No persistence/API route changes. No changes to `application/bank_adapters.py`, `application/import_session.py`, `domain/canonical_line.py`, `domain/statement_layout.py`, `domain/statement_dates.py`, or `domain/settle.py` — if implementation pressure pushes toward touching any of these, stop and reconsider (see Task 3's explicit check).
- This story does not add card-registration UI or API wiring for a new `account_kind` value — that's the existing card-registration flow (Story 4.1/4.2/4.3), unaffected by this story; a user registering a BAC debit card for routing is out of this story's scope (adapter-layer only, per the epic's stated deliverables).

### References

- [Source: ARCHITECTURE-SPINE.md#AD-26 — Per-product date-format contract]
- [Source: ARCHITECTURE-SPINE.md#AD-28 — Real-text row recognition + amount-column role]
- [Source: ARCHITECTURE-SPINE.md#Deferred table — "A real BAC debit adapter ... No longer deferred — scoped as Story 4.9.1"]
- [Source: epics.md#Story 4.9.1: BAC debit adapter (first SIGN_VARIANT implementation)]
- [Source: docs/bank-statement-parsing-playbook.md — SIGN_VARIANT / column_header / construction-time validation sections]
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-09-22.md — this story's trigger/impact analysis and deliverables list]
- [Source: _bmad-output/implementation-artifacts/4-9-bac-credit-real-statement-compatibility-fix.md — sibling adapter's implementation, patterns reused/mirrored throughout this story]
- [Source: api/adapters/bank/bac_credit/adapter.py — structural model for the new adapter]
- [Source: api/domain/statement_row_extraction.py — this story's Task 1 target]
- [Source: api/domain/statement_dates.py:44-78 — `parse_statement_date`'s reference_date nearest-prior-year path (AC #2)]
- [Source: api/domain/line_types.py — no existing debit/credit-account line type, see Dev Notes]
- [Source: api/domain/settle.py:24 — `INCLUDED_LINE_TYPES`, must not be touched without a deliberate decision]
- [Source: bank_data/BAC_DEB_COLONES_jun.pdf — real statement inspected this session for structural evidence (gitignored, never committed)]

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- Calibrated `BacDebitAdapter`'s SIGN_VARIANT x-position ranges empirically against the synthetic fixture's own geometry (measured via a `pdfplumber.extract_words()` round-trip during fixture generation) rather than copying the real-PDF session's numbers verbatim, per AD-28's page-drift warning and the story's own instruction. Real-evidence clusters (DÉBITOS ≈483.9–519.0, CRÉDITOS ≈558.9–594.0) and this fixture's calibrated clusters (DÉBITOS ≈300–360, CRÉDITOS ≈362–420) are not the same numbers — both are internally valid, non-overlapping declarations; a real-PDF deployment would need its own calibration pass against actual statements (out of this story's CI-gated scope).
- `ruff format` reformatted `adapters/bank/bac_debit/adapter.py` and `tests/test_bac_debit_adapter.py` after initial authoring (line-wrapping only, no logic change).

### Completion Notes List

- **Task 1 (domain):** Added `SignVariantRanges` (construction-time-validated x-position range mapping — raises `ValueError` on empty/reversed/overlapping ranges) and `resolve_sign_variant_column` (midpoint-distance fixed-metric resolver) to `domain/statement_row_extraction.py`. 19/19 domain tests pass (10 new).
- **Task 2 (adapter) — open product decisions made explicit, mirroring Story 4.9's convention:**
  - `account_kind = "checking"`: chosen over `"debit"` to avoid confusion with the SIGN_VARIANT "DÉBITOS" column terminology used inside a single row; no existing UI/API code references `account_kind` values beyond `bac_credit`'s `"credit"` and `promerica_stub`'s `"other"`, so this introduces no conflict.
  - New line types `LINE_TYPE_WITHDRAWAL` / `LINE_TYPE_DEPOSIT` added to `domain/line_types.py` (not `LINE_TYPE_PURCHASE`, which is credit-card-specific and gates `domain/settle.py`'s `INCLUDED_LINE_TYPES`). `settle.py` is deliberately **untouched** — no settle-math decision has been made for debit-account rows; this story is adapter-output only.
  - Sign convention: DÉBITOS (money leaving) → `withdrawal`, negative amount. CRÉDITOS (money entering) → `deposit`, positive amount.
  - `detect()`'s filename check is deliberately narrower than `BacCreditAdapter`'s naive `"bac" in filename.lower()`: it requires `"bac"` **and** a debit-product token (`"deb"`/`"debit"`). This avoids `BacDebitAdapter` unconditionally matching any bare `"bac"`-named file on the filename-only pass (which would otherwise silently misassociate real debit statements as unclassified content, or manufacture an `AmbiguousBankAdapterError` collision with `BacCreditAdapter`'s own equally-naive check for filenames containing both tokens). This is a real, documented tradeoff, not a silent choice — see adapter.py's `detect()` docstring/comment; a filename containing both "bac" and "deb"/"debit" (matching the real evidence's own `BAC_DEB_*.pdf` naming) will still collide with `BacCreditAdapter`'s unconditional `"bac"` check and raise `AmbiguousBankAdapterError` on the filename-only pass rather than resolving via content sniff — `bank_adapters.py`'s actual priority-chain code (verified this session) raises immediately on >1 filename match rather than falling through to content, contradicting this story's own Dev Notes description of that behavior. Fail-loud (`AmbiguousBankAdapterError`) is preferred here over a silent wrong pick, but is a known limitation flagged for follow-up rather than silently resolved by touching `bac_credit/adapter.py` or `bank_adapters.py` (both out of this story's declared scope; Task 3's explicit check says stop and reconsider before touching `bank_adapters.py`).
  - Row geometry: `pdfplumber.extract_words()` words are clustered into visual rows by y-position tolerance (3pt) rather than relying on `extract_text()`'s line joins, since AD-28's SIGN_VARIANT resolution needs each amount token's own `(x0, x1)`.
- **Task 3 (registry):** Added to `api/adapters/bank/__init__.py`'s `ADAPTERS` list. Zero edits to `application/bank_adapters.py` or `domain/canonical_line.py` (FR-31/FR-36 proof preserved).
- **Task 4 (fixture):** `generate_bac_debit_fixture.py` builds one statement with a `CUADRO RESUMEN` totals-line decoy (proves the column-header gate), two DÉBITOS rows, and one CRÉDITOS row, and asserts its own geometry against the adapter's declared ranges before writing the PDF (`_verify_geometry`). 25/25 new adapter tests pass, including detect() cross-checks against the real `bac_credit_synthetic.pdf` fixture (neither adapter matches the other's fixture), construction-time `SignVariantRanges` validation, DOLARES currency handling, and two fail-loud paths (missing `Moneda:` field, two-amount-token row).
- **Task 5 (regression):** Updated `test_canonical_line_domain.py::test_line_types_contains_all_ten_prd_named_types` (renamed to `..._plus_debit_account_types`) to include the two new line types — this is the one pre-existing test that hardcoded the full `LINE_TYPES` set and needed updating for the deliberate taxonomy addition. Full suite: 1103 passed (one `test_lists_integration.py` test failed once under full-suite ordering but passed both in isolation and on a second full-suite run — confirmed pre-existing test-order flakiness unrelated to this story's changes, not a regression). `ruff check`/`ruff format` clean. No real BAC debit PDFs were available in this environment for the optional manual smoke test (not a merge gate).

### File List

- `api/domain/statement_row_extraction.py` (modified — `SignVariantRanges`, `resolve_sign_variant_column`)
- `api/domain/line_types.py` (modified — `LINE_TYPE_WITHDRAWAL`, `LINE_TYPE_DEPOSIT`)
- `api/adapters/bank/bac_debit/__init__.py` (new)
- `api/adapters/bank/bac_debit/adapter.py` (new)
- `api/adapters/bank/__init__.py` (modified — registry)
- `api/scripts/generate_bac_debit_fixture.py` (new)
- `api/tests/fixtures/pdf/bac_debit_synthetic.pdf` (new)
- `api/tests/fixtures/pdf/bac_debit_synthetic_goldens.py` (new)
- `api/tests/test_bac_debit_adapter.py` (new)
- `api/tests/test_statement_row_extraction_domain.py` (modified — Task 1 domain tests)
- `api/tests/test_canonical_line_domain.py` (modified — updated `LINE_TYPES` set assertion)

## Change Log

- 2026-09-23: Story implemented end-to-end (Tasks 1–5). Status → review.
