---
topic: finance-helper v1 machine contract
updated: 2026-08-03T18:18
---

- (event by coach) Create intent: distill SPEC from final PRD + UX spines + architecture spine; slug=finance-helper; workspace=_bmad-output/specs/spec-finance-helper; `_bmad` scripts missing — memlog maintained manually in established format
- (constraint by coach) Inherited PRD (binding): self-hosted multi-user web; email+password auth; lists as organizing concept; pluggable detect→split→parse→normalize adapters; Postgres; America/Costa_Rica; fail-loud parse; canonical identity dedup; real statements outside repo; generic vocabulary; v1 thin slice upload→parse→store→shared-expenses
- (constraint by coach) Inherited UX (binding companions EXPERIENCE.md + DESIGN.md): phone+desktop web; Warm Balance + Soft-Ledger; WCAG 2.2 AA; EN+ES; settle-up first; phone swipe / desktop buttons for Individual review; same-price Manual|Parsed only (no keep-both); Simplify never says paid / never records settlement
- (constraint by coach) Inherited architecture spine (adopted companion; AD IDs stable): hexagonal ports-and-adapters; FastAPI+SQLAlchemy+Alembic+Postgres api + Next.js 16 ui + Compose; Import Session/Batch; BCCR FX; remainder→list creator; httpOnly cookie auth; Numeric/Decimal money; synthetic CI fixtures
- (decision by coach) Mode=express distill (inputs rich and final); no field-by-field elicitation
- (decision by coach) Adopt companions (not rewrite): ARCHITECTURE-SPINE.md, EXPERIENCE.md, DESIGN.md — keep AD/IA/visual authority upstream
- (decision by coach) Absorb PRD FRs into CAP intent/success; absorb PRD out-of-scope into Non-goals; architecture HOW stays in adopted spine / Constraints that bend product
- (capability CAP-1) Account auth: signup, sign-in, sign-out, password reset via email; verification only when required for invites/recovery
- (capability CAP-2) Personal list on signup; create/name additional owned lists; membership ACL (peers, no product admin)
- (capability CAP-3) Invite to list by email (registered + unregistered→signup lands on inviting list); SMTP required
- (capability CAP-4) List default even/percentage splits (100% exact); item/receipt overrides (whole-line, absolute sub-amounts, percentage); owner-only list default edit
- (capability CAP-5) Register cards by IBAN + user label; match known IBAN; unknown IBAN blocks import until registered; fixed-list vs review-routing per card
- (capability CAP-6) Upload PDF; detect bank/product (override→filename→content); fail loud on unknown/ambiguous; split multi-statement files
- (capability CAP-7) Bulk or Individual review; Individual outcomes chosen-list / configurable-default / skip; phone swipe + desktop buttons; list picker before high-intent accept
- (capability CAP-8) Explicit payer defaults to current user, editable; manual expense entry mid-cycle
- (capability CAP-9) Statement-scoped parse failure → PDF vs extracted comparison → accept-with-quarantine or dismiss; hand-fix unresolved; durable quarantine after commit; incomplete balance disclosure
- (capability CAP-10) Canonical identity dedup with post-import summary; same-price Manual|Parsed review (no keep-both); confirmed match stores description alias; hand-fixed vs re-parse still prompts keep-manual/take-parsed/keep-both (FR-28); reassign statement; rollback Import Batch
- (capability CAP-11) Shared-expenses view: statement-cycle period; settle-up CRC with Simplify (suggestion only); receipt list newest-first; FX audit originals; only included line types feed settle-up
- (capability CAP-12) Pluggable adapters + CanonicalLine; BAC walmart fixture acceptance bar; Promerica stub multi-statement; dual-column prefer nonzero then CRC
- (decision by coach) Conflict resolution same-price keep-both: UX J7 wins for FR-22 end-of-import same-price (Manual|Parsed only). FR-28 quarantine hand-fix vs later parse retains PRD triad including keep-both — different flow; surface if product wants FR-28 narrowed too
- (decision by coach) Architecture closes prior PRD OPENs: remainder→list creator; BCCR+nearest-prior+flag, no FX override; literal phone swipe required; same-price window list-configurable default ±3 days; synthetic geometry fixtures for CI
- (constraint by coach) Spec-authored companion line-types.md holds taxonomy, settle include/exclude, BAC walmart section map
- (assumption by coach) Manual expense minimum fields = amount + description until product expands (UX ASSUMPTION)
- (assumption by coach) Locale detection/switcher exists without a settings product surface (UX ASSUMPTION)
- (assumption by coach) Email verification optional unless invite/recovery path requires it (PRD FR-4)
- (question by coach) Should FR-28 (hand-fixed vs re-parse) also drop keep-both to match J7, or keep the triad?
- (question by coach) Manual expense fields beyond amount+description for v1?
- (note by coach) Wrapper-only dropped: PRD/UX/arch process ceremony, review HTML decks, stack research narrative, .working journey drafts, brief CLI packaging (superseded)
- (event by coach) SPEC.md + line-types.md derived from this memlog + cited sources
- (constraint by coach) Schema reserves category / category_confidence / category_source; categorization never blocks import (PRD deferred ML)
- (event by coach) Self-validate Pass 1 Coherence: PASS — CAP-1..12 each have intent+success; intents are WHAT; constraints bend design; ≥1 non-goal; success signal demonstrable; CAP IDs unique; prose lean enough for kernel
- (event by coach) Self-validate Pass 2 Preservation: PASS — PRD FRs/NFRs/scope mapped to CAPs/Constraints/Non-goals/line-types; UX/Architecture load-bearing retained via adopted companions + cited constraints; wrapper ceremony (reviews, walkthrough, .working drafts, CLI packaging) logged as dropped
- (note by coach) Wrapper-only content: architecture review HTML/memlog process, UX .working journeys, brief CLI packaging superseded by PRD, stack-options research narrative (pins live in spine)
- (event by coach) Party-mode Anti-Consensus Club walked open questions; user decisions below
- (decision by user) Conflict resolution C2 [ADOPTED]: default pick Manual|Parsed survivor; escape "Not the same expense" keeps both only after confirm warning double-count/overpay risk; escape harder than survivor pick; same rule for same-price (FR-22) and hand-fixed↔re-parse (FR-28) — supersedes PRD keep-both triad and UX "no keep-both without escape"
- (decision by user) Manual create fields [ADOPTED]: amount + description + payer (default current user) + Adjust split disclosure (whole-line / absolute fragments / percentage); list default until disclosure opened
- (decision by user) Swipe vectors pinned [ADOPTED]: right→chosen list (after picker), left→configurable default, down→skip; desktop labeled buttons mirror outcomes
- (decision by user) Locale [ADOPTED]: EN/ES in Account menu; remembered on account; default from browser/Accept-Language on first visit; no Settings product
- (event by coach) SPEC re-derived; PRD/UX/architecture companions patched to match; open questions cleared
