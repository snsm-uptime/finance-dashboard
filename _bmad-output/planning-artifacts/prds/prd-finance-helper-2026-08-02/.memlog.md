---
topic: finance-helper PRD
updated: 2026-08-02T18:04
---

- (event by coach) Create intent: fresh PRD run bound; sources = brief.md, addendum.md, adversarial-remediation-proposals.md (already folded into brief), 26 BAC PDF fixtures in bank_data/
- (decision by user) Stakes calibrated as internal: write the PRD as if others may contribute or use the tool later, not hobby-minimal
- (decision by user) Working mode = Coaching path (walk PM-thinking sections together) rather than Fast path draft
- (event by coach) Env gap: uv/pipx/pip3/pdftotext absent and no python pdf libs, so eco+debit section maps cannot be resolved by fixture inspection in this run; stays an implementation spike
- (decision by user) Coaching entry point = Vision + Features (capability-first); section order follows that spine
- (research by coach) Landscape digest saved to research-landscape.md; covers beangulp/hledger/beancount-import adapter contracts, dedup strategies, python PDF libs, consumer comparables, shared-expense modeling
- (research by coach) Prior art bac_tools (git.posixlycorrect.com/fabian/bac_tools) is an existing pdfplumber-based BAC Costa Rica CREDIT CARD PDF extractor; its schema mirrors our sectioned taxonomy and confirms BAC purchase rows carry a reference plus parallel amount_crc/amount_usd
- (research by coach) beangulp adapter contract (identify/account/date/extract) closely mirrors the brief's detect-parse-normalize; notably extract() receives the EXISTING ledger, so dedup context is passed INTO the adapter rather than applied after
- (research by coach) Dedup prior art disagrees and none of it trusts bank reference IDs: hledger uses a per-file high-water date mark, beangulp fuzzy-matches and only MARKS duplicates for human review, beancount-import writes provenance metadata back. Brief's external_ref-primary identity is a deliberate divergence
- (research by coach) Fixture privacy has a known solution: pdftotext the real statement, anonymize the text, print back to PDF, commit that as the golden fixture (plain-text-accounting community convention)
- (research by coach) Premise validated: Plaid does not cover Costa Rica and Belvo does not either, so statement-file parsing is the only path in this market; Firefly III states PDF will never be supported and Lunch Money's PDF import ships files to a third party
- (research by coach) Per-PRODUCT (account-level) shared flag is nearly unique: only Monarch Shared Views does account-level ownership inheritance; Splitwise, Actual, Firefly all model sharing per-transaction or per-category
- (decision by user) Contributor surface is REAL: intent is to open source this later, so the PRD and product must use generic language throughout - Sebas/Monse/BAC specifics become configuration and in-tree adapters, not hardcoded assumptions
- (change by coach) Open-source intent promotes three items from nice-to-have to candidate v1 requirements: fixture anonymization (cannot ship real BAC statements), generic vocabulary for the shared-expense concept (no personal names in code/schema/CLI), and a documented adapter-authoring contract as a public extension point
- (decision by user) Open source AT v1: the first working version IS the public release, so contributor-facing work (adapter-authoring docs, anonymized/synthetic fixtures, README/LICENSE/CONTRIBUTING, CI) becomes v1 exit criteria
- (decision by user) Real BAC statements live OUTSIDE the repo entirely, referenced by a configured path; nothing real is ever committed
- (change by coach) CONFLICT with brief: brief v1 Scope has no publishing/contributor work and assumes golden fixtures live 'under sample data' in-repo. Open-source-at-v1 plus statements-outside-repo supersedes both; brief Scope needs updating or PRD must explicitly supersede it
- (change by coach) Fixture acceptance forks into two tiers: real statements outside the repo can only be tested on Sebas's machine, while public CI can only run anonymized/synthetic fixtures. Brief's 'walmart fixture acceptance required for v1 exit' must specify WHICH tier gates the release
- (change by user) Peak-state vision is a WEB APP, not a CLI: Streamlit UI at localhost:3000/tools/login, Google account login, drag-and-drop statement upload with a confirmation step, ML categorization inline in the ingest path, Postgres in a separate container, data volume on Sebas's own server outside the repo, and multiple dashboard tabs
- (change by user) Vision introduces LISTS as a first-class concept: shared expenses get a tab PER SHARED LIST (plural), alongside personal lists, and the trends dashboard filters by any individual or multiple selected shared/personal lists. The brief's per-product shared boolean cannot express this
- (change by user) Vision puts ML categorization inside the ingest path (processed then categorized then inserted), while the brief defers categorization entirely to post-v1 and requires that categorization never block import
- (change by user) Vision puts analytics/trends dashboards in the product, which the brief explicitly lists as out of scope for v1
- (change by user) Vision persistence is Postgres in a container with a volume on Sebas's server; brief operational model assumes a local single-file DB with backup/restore as a filesystem file copy - that assumption does not survive Postgres
- (change by user) Vision URL path localhost:3000/tools/login suggests finance-helper is one tool within a larger personal self-hosted site, not a standalone install - deployment model may be docker compose on a homelab rather than pip install
- (event by coach) OPEN: brief describes a CLI product; user's peak vision describes a self-hosted web app. Must resolve whether v1 stays CLI with the web app as roadmap, or v1 becomes a thin slice of the web app. Brief is therefore NOT settled
- (decision by coach) v1 = THIN VERTICAL SLICE of the web app: upload a statement in Streamlit, parse it, store in Postgres, view one shared-expenses tab. Explicitly no CLI, no ML categorization, no trends dashboard in v1
- (decision by coach) Sharing is modeled as a first-class LIST entity from the start (named lists with members and a split ratio, accounts assigned to lists), NOT the brief's per-product boolean. Avoids a later schema migration and is what open-sourcing requires
- (change by coach) SUPERSEDED from brief: the entire CLI surface (import / shared-total / detect commands), the packaging success criterion (pip/pipx/uv tool install running as a real CLI end to end), and the local single-file DB operational model including filesystem-copy backup. Replaced by Streamlit UI + Postgres container
- (change by coach) RETAINED from brief: adapter contract (detect-parse-normalize), import contract and canonical fields, fail-loud on unparseable must-parse sections, canonical identity dedup and idempotent re-import, walmart fixture acceptance as the v1 parsing bar, Promerica stub proving extension
- (event by coach) SCOPE RISK: v1 now combines a thin-slice web app, Postgres, list entities, anonymized fixtures, contributor docs and CI for a public release. Publishing overhead may exceed the product; the open-source-at-v1 decision was made when v1 was a CLI and deserves a re-check
- (event by coach) OPEN: are list members real authenticated users (implying multi-user, which the brief excluded) or just labels for splitting under a single logged-in owner? Decides whether Google auth is v1 scope and whether the schema needs a users table
- (decision by coach) List members are REAL USERS: Monse can log in with her own Google account and see the lists she is on. v1 is therefore MULTI-USER - schema needs users, list membership, and permissions. This directly reverses the brief's 'Multi-user access' out-of-scope line
- (decision by coach) Google login IS v1 scope, not deferred
- (event by coach) SCOPE ALERT: v1 has grown from 'parse PDFs into a local DB and print a total' to 'multi-user authenticated web app with Postgres, list permissions, and statement parsing'. The parsing risk is unchanged and still unproven - real danger of building auth and UI on top of a parser that has never read a statement successfully
- (event by coach) Fixture strategy is v1 work REGARDLESS of publishing: real statements live outside the repo, so the repo has no committed test data at all unless anonymized or synthetic fixtures are built
- (event by user) Requested a walkthrough of what open-sourcing actually costs before deciding publish-at-v1 vs publish-later
- (decision by user) Publishing DEFERRED: build generic with nothing personal committed, open source after v1 works and the adapter contract has survived BAC credit and debit for real. Contributor docs, CI and support policy are post-v1
- (decision by user) Build in PARALLEL rather than parser-first; UI and auth proceed alongside parser work. Coach's scope/sequencing concern noted and consciously accepted
- (event by coach) Vision section settled; drafted PRD Overview, Users, Brief Reconciliation and Scope into prd.md. Next section: Features
- (decision by user) Lists are user-owned and arbitrary in number. Every user gets a personal list by default where their personal expenses go. Users may freely create additional lists - to categorize differently, or to hold data from one specific card
- (decision by user) Any list can be shared with other users. Shared lists support three capabilities: SPLIT, SETTLE, and VISUALIZE - the goal being to simplify the pay-up process between members
- (change by user) SETTLEMENT is a brand new concept not present in the brief or in any prior conversation. The brief only ever promised 'report the monthly shared total'. Settling implies tracking who paid what, a running balance between members, recording settlement payments, and settlement history - a Splitwise-shaped subsystem
- (change by user) Role model drafted earlier is wrong: users are PEERS, not owner-vs-viewer. Every user has personal lists, can create lists, and shares lists with others. Monse uploading her own card statements is implied. 'Owner' is an ops/deployment role, not a product role
- (assumption by user) Uploads target a list (user chooses which list a card's data lands in), rather than sharing being derived from a global account-to-list config - inferred from 'upload data from a specific card to a list', needs confirmation
- (decision by coach) v1 shows BALANCES ONLY - what each member owes for the period. Actually settling up happens outside the app; settlement lands in v2
- (decision by coach) Settlement, when it arrives, is a RUNNING LEDGER: balances accumulate across months, payments reduce them, debt history is visible over time (Splitwise-style). NOT per-month closeout
- (decision by coach) Card-to-list cardinality is ONE for v1, but the design must not forbid many later
- (assumption by coach) Two design consequences follow: (1) balances must be computed from per-transaction share allocations that a future settlements table can reduce, not from a month-scoped total, or the running ledger needs a rewrite; (2) card-to-list association should be a join table with a v1 uniqueness constraint rather than a list_id column on the account
- (decision by user) One PDF can contain MULTIPLE statements destined for different lists. The ingest unit is therefore a statement, not a file - a single upload decomposes into N statements, each independently assignable
- (decision by user) Upload has two review modes chosen by checkbox before the review screen: CHECKED assigns the whole PDF to one list picked from a dropdown; UNCHECKED opens a Tinder-style swipe screen reviewing statements one by one
- (decision by user) In swipe mode: swipe to accept moves the statement to a chosen list; swipe to cancel sends the statement to the user's personal list by default
- (change by coach) ADAPTER CONTRACT IMPACT: brief assumed one file maps to one product/statement (detect-parse-normalize on a file). Multi-statement PDFs insert a SPLIT step - detect, split into statements, then parse each with its own product identity. The Promerica stub and contract tests must cover the multi-statement case
- (event by coach) GAP: there is no reject path. Swipe-to-cancel means 'send to personal', not 'do not import'. A statement the user does not want stored at all has nowhere to go
- (event by coach) FEASIBILITY RISK: Streamlit is a form-and-rerun framework with server round-trips; a Tinder-style swipe interaction is not native to it and would need a custom component or a different front end. Also swipe implies touch, but the stated environment is a desktop browser at localhost:3000
- (decision by user) Parse failure means ALERT the user and DO NOT PROCESS - no partial or degraded import of something that did not parse correctly
- (decision by user) Swipe gestures settled: RIGHT accepts the statement into a chosen list, LEFT accepts it into the user's personal list (the default path), DOWN skips the statement entirely so it is never stored
- (change by coach) Swipe-down closes the reject-path gap flagged earlier: users can now decline a statement outright rather than being forced to import everything
- (event by coach) OPEN: failure granularity is ambiguous. If one statement inside a multi-statement PDF fails to parse, is the whole upload rejected or only that statement? Determines whether the user must fix and re-upload everything or can proceed with the rest
- (event by coach) OPEN: does strict no-partial-import apply to BEST_EFFORT sections too? Brief policy was must_parse sections fail the import while best_effort sections warn and quarantine. 'Alert and do not process' may override that, which would make a cosmetic change in a minor BAC section block an otherwise good import
- (decision by user) Failure granularity is PER STATEMENT: a statement that fails to parse is rejected on its own and the rest of the upload proceeds
- (decision by user) NEW FEATURE - side-by-side verification: when parsing fails, render the original PDF alongside the items that were extracted so the user can compare them directly. On mobile the PDF occupies the bottom half of the screen
- (decision by user) The user can dismiss an entire file outright, in addition to the per-statement swipe-down skip
- (change by coach) MOBILE IS A TARGET. 'render the pdf on the bottom half of the screen for mobile devices' establishes phones as a real form factor, not just the desktop localhost browser described earlier. This retroactively explains the swipe gesture
- (change by coach) FRAMEWORK RISK ESCALATED: Streamlit must now deliver gesture-driven review, in-browser PDF rendering, and a responsive mobile layout. Streamlit reruns on every interaction, has no native gesture support, no first-class PDF viewer, and a weak mobile story. Three independent UX requirements now pull against the chosen framework
- (decision by user) Duplicates are absorbed silently and reported afterwards, e.g. imported twelve new and skipped forty-three duplicates
- (decision by user) Correction supports BOTH direct reassignment of a statement to another list and removal of a whole import batch as an escape hatch. The brief's journaled-batch model is therefore RETAINED
- (event by coach) OPEN: after viewing the side-by-side comparison, can the user accept the partial parse, or is dismissing the only option? Decides whether the product is strict-with-diagnostics or strict-with-human-override
- (decision by user) From the comparison view the user may ACCEPT WITH QUARANTINE: rows that parsed import normally, rows that did not are stored separately as unresolved rather than discarded. Dismissing the file remains available
- (decision by user) The comparison view is FAILURE-ONLY, not a general spot-check tool for statements that parsed cleanly
- (decision by user) Streamlit is a PREFERENCE, not a lock. Architecture may choose whatever actually delivers gesture review, PDF rendering and mobile layout
- (change by coach) This reconciles with the brief rather than overriding it: the brief already had best-effort sections warn and quarantine. Generalized now to any failed section, with a human deciding via the comparison view instead of a static per-section policy deciding in advance
- (event by coach) CORRECTNESS RISK: quarantined purchase rows understate a shared list balance. Accepting with quarantine means the user could under-bill a list member and never know. Balances must disclose that they are computed over a statement with unresolved rows
- (assumption by coach) Quarantine resolves naturally by re-upload once the parser is fixed - dedup absorbs the already-imported good rows and the previously-quarantined rows now parse. This only holds if canonical identity is stable, which makes dedup correctness load-bearing for quarantine recovery
- (decision by user) REVERSAL - Google login is dropped. Authentication is email and password. This overrides both the peak-vision 'log in with my google account' and the earlier decision that Google auth was v1 scope
- (decision by user) Invitations are sent by EMAIL. Two templates: one for an address already registered, and a different one for an unregistered address that guides the recipient into creating an account. On account creation the site redirects them to the list they were invited to
- (change by coach) NEW INFRASTRUCTURE: transactional email is now a v1 dependency (SMTP configuration, deliverability, templates). Also password storage and hashing, credential reset, and probably email verification - security responsibilities that OAuth previously delegated to Google
- (change by coach) Publishing tradeoff shifts: dropping Google removes the per-deployer Google Cloud project and consent-screen burden flagged earlier, but replaces it with SMTP configuration for every self-hoster - a smaller but non-zero onboarding cost
- (decision by user) Split is PER MEMBER: explicit shares or percentages per person on a list, so three-way and uneven splits work. Not a single list-level ratio
- (decision by user) Quarantined rows can be MANUALLY EDITED: the user types correct values from the PDF and resolves the row without touching the parser
- (change by coach) Manual edit means human-authored rows enter the ledger, so rows need PROVENANCE - parser-derived versus hand-entered - for both trust and debugging
- (event by coach) TRAP: manual resolution collides with re-upload recovery. If a user hand-fixes a row and later fixes the parser and re-uploads, the parsed row may not match the hand-typed row's canonical identity (description normalization, amount rounding), producing a duplicate. Needs a policy: manual rows either lock their identity slot or are superseded by parsed rows
- (change by coach) The comparison view is no longer read-only: it must support editing rows against a rendered PDF on a phone. Materially harder UI than displaying a diff
- (event by coach) User skipped the manual-row policy, account-surface scope, and share-arithmetic questions. All three remain open in the PRD; none were assumed
- (event by coach) RESUME POINT: Vision and Statement ingestion are drafted. Still unwritten - list management detail, the shared-expenses view, the parsing and adapter requirements inherited from the brief, non-functional requirements, and the numbered FRs. Then Finalize
- (event by user) Session paused by user. PRD left at status draft so a future bmad-prd run offers it as a resumable in-progress run
