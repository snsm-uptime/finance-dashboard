---
topic: finance-helper architecture spine
updated: 2026-08-03T19:20
scope: finance-helper v1 thin vertical slice (upload → parse → store → shared-expenses) plus extension surface for adapters
purpose: build-substrate spine for implementers/agents, plus HTML+SVG walkthrough for future-self alignment
altitude: feature
---

- (event by coach) Create intent: fresh architecture run bound at architecture/architecture-finance-helper-2026-08-03; sources = prd-finance-helper-2026-08-02/prd.md (final), ux-finance-helper-2026-08-03 EXPERIENCE.md+DESIGN.md (final); no prior architecture run; no project-context.md; `_bmad` scripts missing — memlog maintained manually in established format
- (decision by user) Working mode = Coaching path
- (decision by user) Deliverable = spine + interactive HTML+SVG walkthrough deck (audience = future-self / solo implementers, later OSS contributors)
- (constraint by coach) Inherited from PRD (binding, not re-decided): self-hosted multi-user web app; email+password auth; first-class Lists; pluggable detect→split→parse→normalize adapters; Postgres; America/Costa_Rica dates; fail-loud parse; canonical identity dedup; real statements outside repo; generic vocabulary; Streamlit is preference-only not commitment
- (constraint by coach) Inherited from UX (binding): phone + desktop web; Warm Balance + Soft-Ledger hybrid owned by DESIGN.md; UI kit unspecified — architecture chooses stack that can deliver phone swipe + desktop buttons + PDF comparison; spines win over mocks
- (question by coach) Load-bearing open from PRD/UX for this run: design paradigm + web/app stack (must support gestures, PDF side-by-side, responsive); remainder-cent assignment rule; FX source/missing-date; fixture production for positional parsing; process topology (mono vs worker); auth session/security posture
- (decision by user) Design paradigm = hexagonal / ports-and-adapters [ADOPTED]: domain (lists, users, commit, dedup, settle math) has no imports from UI or PDF libs; bank adapters and web UI are adapters; ingest detect→split→parse→normalize→review→commit is a pipeline *inside* the application/adapter ring, not the top-level paradigm
- (decision by coach) Rejected as top-level paradigm: pipes-and-filters-first (keeps lists/auth demoted) and classic layered alone (weaker plug-in boundary for banks)
- (decision by user) Language shape = Python owns domain + PDF adapters; browser UI is a real SPA client over HTTP (not Streamlit, not TS-owned domain)
- (decision by user) Cold-start starter = fastapi/full-stack-fastapi-template (v0.10.0 family): FastAPI + SQLModel + PostgreSQL + React/Vite/TS + Docker Compose + JWT email/password — [ADOPTED]; verified web 2026-08-03
- (decision by coach) Rejected for cold-start: Streamlit (PRD already weakened); FastAPI+Next.js (extra glue); full hand-roll (rebuilds auth/Compose)
- (constraint by coach) Template UI (shadcn/Tailwind defaults) is NOT brand authority — Warm Balance + DESIGN.md overrides; template is substrate for auth/Compose/SQLModel/React client generation
- (assumption by coach) v1 parse runs in-process in the API service (no separate worker) unless latency forces later split — confirm with user
- (decision by user) Ingest runtime v1 = in-process synchronous in the API service: upload path detects/splits/parses before review; no job queue in v1 [ADOPTED]
- (decision by user) Durable state owner = PostgreSQL only for app data (users, lists, staged review, commits, quarantine, balances inputs); statement PDF bytes live on local disk/volume with path references in Postgres — not bytea, not S3 in v1 [ADOPTED]
- (decision by user) Split remainder (subunit) assignment = always to the list creator, after floor-division of shares that sum to 100% [ADOPTED] — closes PRD [OPEN — architecture] remainder
- (decision by user) FX = BCCR daily rate keyed to purchase/statement date; if that date missing, use nearest prior published BCCR date and flag the converted row as rate-fallback [ADOPTED] — closes PRD [OPEN — architecture] FX source/missing-date (override-allowed remains open unless tightened)
- (decision by user) Money representation = Postgres NUMERIC as source of truth; domain uses Decimal; never float for amounts [ADOPTED]
- (decision by user) Fixture strategy = synthetic PDFs with known geometry + golden expected rows exercise positional extractors; real statements remain outside repo [ADOPTED] — closes PRD fixture-production OPEN for the CI path
- (change by user) SUPERSEDED frontend of starter: product UI is Next.js 16 (create-next-app App Router + TS + Tailwind), not the template Vite React app [ADOPTED]
- (decision by user) Stack = FastAPI + SQLModel + PostgreSQL backend (template-inspired substrate) + Next.js 16 frontend + Docker Compose (+ Traefik family) on operator homelab [ADOPTED]; Streamlit out
- (decision by user) Dependency rule = bind allowed/forbidden shortlist: Next→HTTP API only; application↔domain; adapters return data / repos persist; forbidden: Next→Postgres/SQLModel, Next→parsers, adapters→commit/lists, adapter↔adapter, domain→frameworks/pdf [ADOPTED]
- (decision by user) Auth delivery = httpOnly secure cookie JWT (or equivalent) on same-origin via Compose/Traefik; not Bearer-in-localStorage [ADOPTED]
- (decision by user) Deploy = Docker Compose on operator homelab including Traefik-class reverse proxy for same-origin HTTPS/path routing [ADOPTED]
- (decision by user) Review staging model = Import Session aggregate: upload creates a staged session (statements + candidate rows); commit promotes into the durable ledger; discard drops the session [ADOPTED]
- (decision by user) Pacing = sweep remaining PRD [OPEN — architecture] items before drafting the spine
- (decision by user) FX override = none in v1; BCCR (+ nearest-prior fallback + row flag) is authoritative [ADOPTED] — closes residual of PRD FX OPEN
- (decision by user) Literal swipe = required on phone Individual review; desktop uses buttons [ADOPTED] — hardens UX resolution of PRD UX/architecture OPEN
- (decision by user) Same-price date window = list-configurable; product default ±3 calendar days from posted date [ADOPTED]
- (event by coach) User paused draft to review stack-options.md (alternate stack research in this workspace) before finalizing stack binding
- (decision by user) After reviewing stack-options.md: adopt its preferred Compose stack as binding — db (Postgres 16) + api (FastAPI/Uvicorn) + ui (Next.js 16 standalone); pdfplumber on api; react-pdf + @use-gesture on ui; aiosmtplib; pytest golden fixtures; Node-primary alternate explicitly NOT selected [ADOPTED]
- (decision by user) ORM = SQLAlchemy 2 + Alembic + separate Pydantic API schemas (not SQLModel-as-HTTP-shape) [ADOPTED]
- (change by coach) Starter framing refined: not wholesale clone of full-stack-fastapi-template UI/ORM — template remains inspirational for Compose/auth patterns; selected pin table lives in stack-options.md Recommended stack
- (decision by coach) Auth mechanism stays httpOnly cookie on same-origin proxy/BFF; library choice fastapi-users OR custom argon2+signed cookie sessions per stack-options — both satisfy cookie AD; JWT-in-localStorage remains rejected
- (constraint by coach) Statement PDF bytes: retain coaching decision — on-disk/volume paths referenced from Postgres (not forced into DB bytea); stack-options 'tmp or DB' phrasing narrowed by this AD
- (event by coach) Draft ARCHITECTURE-SPINE.md distilled from memlog + stack-options.md pins; status=draft for user review (not yet finalized; reviewer gate and HTML walkthrough pending)
- (decision by user) Git branch per user story = `<type>/<epic>/<us-id>` where type ∈ feat|fix|chore|docs|refactor|test|ci (Conventional Commit types) [ADOPTED]
- (decision by user) Semantic versioning = tags releases of the Compose app as a whole (vMAJOR.MINOR.PATCH) [ADOPTED]
- (decision by user) TDD = hard red→green for parsers/domain; UI may be test-after with an explicit coverage floor [ADOPTED]
- (decision by user) CI gate before merge to main = required checks: lint + api pytest (incl. fixture goldens) + ui typecheck/lint + critical ui tests; Playwright e2e not required on every PR in v1 [ADOPTED]
- (event by coach) Process ADs to be merged into draft ARCHITECTURE-SPINE.md
- (event by coach) Reviewer Gate run: lint (frontmatter path tokens), rubric-walker, version-reality, adversarial-divergence, reconcile-prd, reconcile-ux → reviews/*
- (change by coach) Gate autofixes applied: AD-4 batch boundary per Statement; AD-7 FX materialize-at-commit; AD-12 UX companions binding; AD-16 CanonicalLine; AD-17 quarantine ownership; AD-18 domain dedup; AD-19 list ACL; AD-20 cards; AD-21 v1 settle vs payments; AD-22 ops envelope; stack aiosmtplib≥5.1.2; react-pdf owns pdfjs; sources paths resolved; status→final
- (decision by coach) Surfaced-as-deferred not blocked: BCCR wire format spike; UI coverage %; auth library pick; Playwright e2e
- (event by coach) HTML walkthrough written to walkthrough.html (8 slides, audience future-self)
- (event by coach) spine finalized

- (decision by user) Spec open-questions walk [ADOPTED]: conflict C2 (Manual|Parsed + confirmed Not the same expense) for FR-22 and FR-28; manual create amount+description+payer+Adjust split; swipe R/L/D pinned in AD-9; locale Account menu EN/ES remembered
- (event by coach) Update intent resumed 2026-08-14 from architecture-finance-helper-2026-08-03 memlog; coaching path; `_bmad/scripts/memlog.py` still absent — appends remain manual
- (direction by user) Bind centralized font delivery, Tailwind-compatible, consumed via classes or HTML tags so TSX stays thin (custom styles/params only) — not a how-to dump of file paths
- (decision by user) Deliverables = updated spine + refreshed human-facing walkthrough (existing walkthrough.html audience = future-self / Amelia)
- (constraint by coach) Inherited, not reopened: DESIGN.md owns faces (Petrona/Manrope) and type roles; AD-12/AD-23 own look and Tailwind-vs-SCSS. This update only binds *how faces enter and are consumed*
- (constraint by coach) Class-attribute order stays out of the spine (house style → project-context)
- (question by coach) Consumption fork: (1) HTML-tag-primary type, (2) family utilities only (`font-ui`/`font-brand`), (3) tag defaults for document face + DESIGN.md type-role utilities (`type-brand`, `type-strip-amount`, …) as the product API — coach leans (3)
- (assumption by coach) Tailwind v4 `@theme --font-*` generates family utilities; `@utility` registers type-role classes that participate in variants — verified docs 2026-08-14. next/font remains the loader (already in `ui/app/layout.tsx`)
- (decision by user) Type delivery = AD-24 [ADOPTED]: next/font loads faces once at root layout; Tailwind `@theme --font-ui/--font-brand` generate family utilities; DESIGN.md roles become `@utility type-*` (full recipe); `body`/form controls inherit UI face only; TSX does not set fonts (primitives own the role class; else one `type-*` class). HTML tags are not the primary type API. Binds: all `ui` type. Prevents: per-file family strings, inline fontFamily bags, tag→face maps that fight DESIGN.md, next/font vs `@theme` sharing one CSS variable.
- (decision by user) Rejected: HTML-tag-primary type; family-only utilities with local size/weight (would leave DESIGN.md metrics to fork per SCSS file)
- (constraint by coach) AD-12/AD-23 unchanged in force — AD-24 does not pick faces or replace Tailwind-vs-SCSS; it only binds consumption. Existing inline `fontFamily` / SCSS `font-family` currently violate AD-24; migration is implementation, not a new decision
- (decision by coach) No `font-sans`/`font-serif` alias — product names only (`font-ui`/`font-brand`) to avoid two names for one face
- (version by coach) Tailwind v4 `@theme --font-*` → `font-*` utilities and `@utility` for custom classes confirmed current on tailwindcss.com (2026-08-14); next/font already in live `ui/app/layout.tsx`
- (event by coach) Reviewer Gate 2026-08-14: lint ok; rubric-walker pass-with-fixes; adversarial 13 pairs; reconcile-design aligned with manner gaps; version-reality pass (inline, subagent interrupted)
- (change by coach) Gate autofixes applied to AD-24: --face-* vs --font-* named; one --type-* registry; DESIGN.md catalog snapshot; recipe includes transform + tabular nums; money runs use money roles; primitive-mandatory; no generic Text; body role on body; grandfather do-not-extend; Forbidden font-sans + type-*+metrics; only root next/font. AD-12 addendum: DESIGN.md component → Soft-Ledger primitive. AD-23: type metrics not and/or
- (event by coach) walkthrough.html slide 09 type delivery; header AD-1…24
- (event by coach) spine finalized (update 2026-08-14)
