---
topic: Docs/tutorials page (/docs) UX refactor
updated: 2026-09-02T23:54
---

- (decision) Scope: refactor /docs (Tutorials & guides) page UX. Problems: wall of text, no navigation/search, generic non-valuable content, missing 'when to use' context.
- (decision) Audience: both new users exploring pre-signup AND active users stuck mid-task looking up a quick reference — page must serve both browse and lookup.
- (decision) Content upgrade: keep 'why/edge cases' prose, add numbered mini-walkthroughs (steps), lightweight SVG diagram illustrations (same linear stroke-1.5 accent language as landing FeatureCard icons, not real screenshots to avoid maintenance burden), and deep links from each entry to the actual app route where the action happens.
- (decision) Navigation: collapsible accordion per section (Lists / Cards & imports / Budgets), collapsed by default, expand-on-demand rather than a fixed sidebar/TOC.
- (decision) Entry point: add a '?' IconButton labeled 'Learn more' linking to /docs in TWO places — (1) header.trailing slot of the authenticated /home page via AppShell's existing IconButton pattern, (2) public landing page (ui/app/page.tsx), replacing the current plain text link 'Tutorials & accessibility guide'.
- (assumption) Deep-link targets are the existing top-level routes (/lists, /budgets, /cards, /upload) since no dedicated '/new' subroutes exist in the app tree — the create action likely lives as UI state within those pages.
- (decision) Design inherits from the existing 'finance-helper' DESIGN.md spine (final, ux-finance-helper-2026-08-03) via {finance-helper.*} token references rather than defining a new visual identity.
- (change) Entry-point decision refined (supersedes prior 'two places' note): contextual '?' IconButton (tooltip 'Learn more', no visible label) in the top-right corner of every authenticated page that has matching /docs material (Lists, Budgets, Cards, Upload/import screens) — NOT on the public landing page, which keeps its own existing text link. Each button deep-links to that page's specific section in /docs (hash anchor, e.g. /docs#lists), which auto-expands that accordion section on load.
- (event) Discovery closed — user confirmed no further additions.
- (event) DESIGN.md and EXPERIENCE.md drafted for docs-tutorials-page, inheriting from finance-helper base spine.
- (event) Accessibility reviewer ran: 1 critical, 2 high, 2 medium, 3 low. All 8 resolved by editing EXPERIENCE.md (focus management on deep-link arrival, non-color+persistent highlight, prefers-reduced-motion, focus-ring on help-icon-button, aria-hidden panel sync, illustration authoring rule, deep-link placement rule, touch-target spacing).
- (event) Structural polish pass: merged redundant reduced-motion bullet into Deep-linked/Keyboard-focus state bullets. Prose pass: no issues found. Spines finalized.
